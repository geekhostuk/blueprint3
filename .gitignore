# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# local secrets for the quote form (Resend key) — never commit
.dev.vars

# cloudflare / wrangler local state
.wrangler/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
.vscode/

# ── Not part of the site ────────────────────────────────────────────────
# Brand source material and internal docs. These stay local: the repo is
# public, and none of it is needed to build or run the site. The only
# derived assets the site needs (public/assets/logo-cube.png, the favicons,
# public/og.png) are committed.
BLUEPRINT3-BRAND-GUIDELINES.md
BRAND-AND-CONTENT.md
Standard_Logo_RevA_V1_v0.png
StickerFront.jpg
StickerBack.jpg
*.pdf

# Agent/editor instructions — local workflow notes, not site code.
# The dev-server caveat they describe is also in README.md.
AGENTS.md
CLAUDE.md
