# Normalise line endings so the repo stays consistent across platforms.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.ico binary
*.jpg binary
*.pdf binary

# Generated by `npm run types`; collapse it in diffs and exclude it from
# language stats.
worker-configuration.d.ts linguist-generated=true
package-lock.json linguist-generated=true
